<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_login_constraint"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/activity_login_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/Heruri"
        android:maxLines="1"
        android:textSize="110sp"
        android:textStyle="bold"
        android:textAlignment="center"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/activity_login_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/inputYourPassword"
        android:maxLines="1"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_margin="30dp"
        app:layout_constraintTop_toBottomOf="@id/activity_login_title" />

    <EditText
        android:id="@+id/activity_login_username"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="@string/id"
        android:inputType="number"
        android:paddingStart="20dp"
        android:elevation="4dp"
        android:background="@drawable/all_round"
        app:layout_constraintVertical_bias="0.1"
        android:layout_margin="40dp"
        app:layout_constraintTop_toBottomOf="@id/activity_login_title"
        app:layout_constraintBottom_toBottomOf="parent" />

    <EditText
        android:id="@+id/activity_login_password"
        android:layout_width="match_parent"
        android:inputType="textPassword"
        android:layout_height="60dp"
        android:hint="@string/password"
        android:paddingStart="20dp"
        android:background="@drawable/all_round"
        android:elevation="4dp"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="40dp"
        app:layout_constraintTop_toBottomOf="@id/activity_login_username" />

    <Button
        android:id="@+id/activity_login_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/login"
        android:layout_margin="40dp"
        app:layout_constraintVertical_bias="0"
        android:background="@drawable/button_round"
        android:elevation="8dp"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/activity_login_password"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/activity_login_notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/loginDescription"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_margin="30dp"
        app:layout_constraintTop_toBottomOf="@id/activity_login_button" />

    <TextView
        android:id="@+id/activity_login_reportEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bugReport"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_margin="30dp"
        app:layout_constraintTop_toBottomOf="@id/activity_login_notification" />

</androidx.constraintlayout.widget.ConstraintLayout>