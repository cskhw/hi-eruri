<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.wiserock.heruri.MainActivity">

    <data>

        <variable
            name="course"
            type="com.wiserock.heruri.navigation.course.LectureViewModel" />

        <variable
            name="pos"
            type="Integer" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:background="@color/lightRed"
        android:onClick="@{() -> course.setOnClickCourseListener(context, course.courseList[pos].href)}"
        android:layout_height="wrap_content">

        <TextView
            android:textAlignment="center"
            android:text="@{course.courseList[pos].name}"
            android:id="@+id/item_course_text1"
            android:textSize="20sp"
            android:layout_marginStart="30dp"
            android:layout_marginTop="10dp"
            android:layout_width="wrap_content"
            android:textColor="@color/white"
            android:layout_height="40dp"
            android:textStyle="bold"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/item_course_text2" />

        <TextView
            android:text="@{course.courseList[pos].professor}"
            android:id="@+id/item_course_text2"
            android:layout_marginStart="30dp"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:textColor="@color/white"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/item_course_text1" />


        <TextView
            android:text="@{course.courseList[pos].deadline}"
            android:id="@+id/item_course_text3"
            android:layout_marginTop="30dp"
            android:layout_width="wrap_content"
            android:textStyle="bold"
            android:layout_height="40dp"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:layout_marginEnd="20dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <LinearLayout
            android:layout_marginHorizontal="10dp"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            app:layout_constraintTop_toTopOf="parent"
            android:background="@color/darkGray"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.9" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
